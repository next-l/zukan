<%= form_for(@controlled_access_point) do |f| %>
  <% if @controlled_access_point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@controlled_access_point.errors.count, "error") %> prohibited this controlled_access_point from being saved:</h2>

      <ul>
      <% @controlled_access_point.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :base_access_point %><br />
    <%= f.text_area :base_access_point %>
  </div>
  <div class="field">
    <%= f.label :type_of_controlled_access_point %><br />
    <%= f.text_field :type_of_controlled_access_point %>
  </div>
  <div class="field">
    <%= f.label :language_of_base_access_point %><br />
    <%= f.text_field :language_of_base_access_point %>
  </div>
  <div class="field">
    <%= f.label :source_of_controlled_access_point %><br />
    <%= f.text_area :source_of_controlled_access_point %>
  </div>
  <% if @controlled_access_point.name_id %>
    <div class="field">
      <%= f.label :name_id %><br />
      <%= link_to Name.where(:id => @controlled_access_point.name_id).first.try(:name_string), Name.where(:id => @controlled_access_point.name_id).first %>
    </div>
  <% end %>
    <%= f.hidden_field :name_id %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
